#include "main.h"

/**
 * _strcat -  concatenates two strings
 * @dest: is a parameter
 * @src: is a parameter
 * Return: _strcat
 */
char *_strcat(char *dest, char *src)
{
	_strcat = *dest + *src;
	return (_strcat);
}
