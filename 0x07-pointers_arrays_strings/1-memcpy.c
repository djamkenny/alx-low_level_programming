#include "main.h"
/**
 * _strchr - Entry point
 * @s: input
 * @c: input
 * Return original_dest
 */
char *_strchr(char *s, char c)
{
	char *original_dest = dest;

	while (n--)
	{
		*dest++ = *src++;
	}
	return (original_dest);
}
